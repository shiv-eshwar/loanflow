# Copy to .env and fill locally. Never commit real values.

# Optional. If set, enrich uses gpt-4o-mini (zod JSON) + embeddings RAG.
# Labels still come from rules. Never commit this file as .env.
OPENAI_API_KEY=

# Required only for --file-issue / listing live GitHub issues.
GITHUB_TOKEN=
GITHUB_REPOSITORY=shiv-eshwar/loanflow
